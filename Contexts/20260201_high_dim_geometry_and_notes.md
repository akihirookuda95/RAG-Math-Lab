# セッションコンテキスト（高次元幾何・線形代数基礎）_2026-02-01

## 1. 目的（Goal）
- RAG（ベクトル検索等）の「なぜ効くか」を数学（線形代数→高次元幾何→情報理論…）で腹落ちさせる
- Week1-2（線形代数）の基礎を教材＋検算コードで開始し、次元の呪い（距離/角度の集中）まで体感する

## 2. 現在地（Current status）
- 完了:
  - 線形代数基礎の教材と検算コードを整備（内積・コサイン類似度・射影）
  - 高次元幾何（ほぼ直交・距離の集中）の実験コード作成＆写経完了
  - 実験結果をノートに貼り付け、数値の読み方（特に `std/mean`）で解説を追記
- 進行中: N/A

## 3. 重要な決定（Key decisions）
- **高次元実験は別スクリプトに分離**
  - **結論**: `scripts/high_dim_geometry.py` として分離
  - **理由**: `scripts/linear_algebra_basics.py` は「基礎の検算」、高次元は「次元の呪い体感」で目的が異なるため
  - **記録先**: `scripts/high_dim_geometry.py`
- **高次元幾何の理解はノート化して蓄積**
  - **結論**: `docs/notes/2026-02-01_high_dim_geometry.md` を作成し、Q&A（誤解→修正）も含めて整理
  - **理由**: 次セッションでも同じ誤解に戻らず、実験結果と結論の対応を短時間で復元できるようにするため
  - **記録先**: `docs/notes/2026-02-01_high_dim_geometry.md`（特に「7. あなたの実験結果の読み方」）
- **過去コミットのメッセージを書き換え**
  - **結論**: 旧コミット `3deb349...` のメッセージを「本リポジトリの概要書を作成」に変更（履歴書き換え）
  - **理由**: リポジトリの位置付けが分かるようにするため
  - **記録先**: `git log` 上では新コミットID `6e2b23f...` になっている（書き換えによりIDが変化）

## 4. 未決事項・不明点（Open questions / Unknowns）
- **次の学習ステップの具体設計（Week3-4 情報理論）**
  - **なぜ重要か**: BM25のIDF（log(N/df)）を理論導出する本筋に入るため
  - **何が分かれば決められるか**: どの導出ルートで進めるか（確率的関連性モデル寄り／符号化・情報量寄り）

## 5. 実装・アーキテクチャの要点（Architecture / Implementation notes）
- `docs/` は教材・ノート（文章中心）
- `scripts/` は検算・実験（NumPy）
- 依存は `requirements.txt`（numpy）
- `.gitignore` により `.venv/` や `.cursor/` 等の生成物をコミット対象から除外

## 6. 関連ファイル（Files touched / relevant files）
  - `docs/notes/week1_linear_algebra_basics.md`
  - **なぜ重要か**: Week1-2の基礎教材（内積・コサイン類似度・射影）
  - **何が変わったか**: 数式表記を `$...$ / $$...$$` に統一して可読性改善
- `scripts/linear_algebra_basics.py`
  - **なぜ重要か**: 手計算例とランダム検算（cosと射影の性質）をNumPyで再現
  - **何が変わったか**: dot/norm/cos/projection を提供
- `scripts/high_dim_geometry.py`
  - **なぜ重要か**: 次元の呪い（角度/距離の集中）を体感する実験
  - **何が変わったか**: ユーザが写経して完成、dごとの cos/dist mean/std を出力
- `docs/notes/2026-02-01_high_dim_geometry.md`
  - **なぜ重要か**: 高次元幾何の全体解説＋Q&A＋実験結果の解釈（`std/mean`）
  - **何が変わったか**: 実験ログ（d=2..8192）を追記し、その意味を言語化
  - `docs/todos/tomorrow_tasks.md`
  - **なぜ重要か**: 学習TODOのチェックリスト
  - **何が変わったか**: 明日の実施項目を整理
- `README.md`
  - **なぜ重要か**: リポジトリの使い方（教材とスクリプトの導線）
  - **何が変わったか**: Week1-2の導線と実行手順を追記

## 7. 評価文脈（Evaluation context）
- データセット: N/A（今回はランダムベクトルでの幾何実験のみ）
- 指標:
  - cos: mean/std（d増加でstdが縮む→0付近に集中）
  - dist: mean/std と **std/mean**（相対揺れが減る→距離が集中）
- 注意:
  - 実験は「ランダム点の幾何」であり、学習済みembeddingの意味構造とは別（ただし直観の土台になる）

## 8. 次回やること（Next steps）
- [ ] `docs/notes/2026-02-01_high_dim_geometry.md` を踏まえ、**「なぜRAGで角度/内積がよく使われるか」**を自分の言葉で3〜5行に要約（exit: 説明文が完成）
- [ ] Week3-4（情報理論）へ移行し、エントロピー→相互情報量→BM25のIDF導出の学習ノートを作成（exit: 導出の骨子が書ける）
- [ ] BM25のIDFが `log(N/df)` になる導出ルートを1つ決め、式変形を追える形にする（exit: 手で追える）
- [ ] 必要なら、簡単な toy corpus で df と IDF の挙動をコードで確認（exit: 数値例で説明可能）

## 9. リスク（Risks / gotchas）
- **「ランダム点の幾何」と「学習embeddingの性質」を混同する**
  - 回避: ノートで「ランダムのデフォルト」と「学習での偏り」を明確に区別する
- **距離の集中を std の絶対値だけで誤読する**
  - 回避: `std/mean`（相対揺れ）で読む、とノートに明記済み
- **履歴書き換えによるリモートとの不整合**
  - 回避: 共有済みなら force push が必要。push は依頼があるまで実施しない

## 10. 参考（References, optional）
- N/A

